


// let cookieCount = 0;

// // Add an event listener to the Cookie-Count element
// document.querySelector(".Cookie-Count").addEventListener("click", incrementCounter);

// function incrementCounter() {
//     cookieCount++; // Increase the counter by 1
//     document.querySelector(".cookie-count").innerText = cookieCount; // Update the displayed count
// console.log(cookieCount);

// }
// incrementCounter();